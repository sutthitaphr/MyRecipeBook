<p style="color: green"><%= notice %></p>

<%= render @recipe %>

<div>
  <%= link_to "Edit this Recipe", edit_recipe_path(@recipe) %> |
  <%= link_to "Back to Recipes", recipes_path %>

  <%= button_to "Delete this Recipe", @recipe, data: { turbo_confirm: "Are you sure?" }, method: :delete, class:'btn btn-danger' %>
</div>
